# Security — never commit secrets
.env
.env*
*.pem
*.key
credentials.*

# macOS
.DS_Store

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build output
dist/
.vite/

# Rust / Tauri
target/
src-tauri/target/
src-tauri/gen/

# Revenant sidecars — auto-added by Revenant on first annotation
*.annotations.json
*.review.md

# Workflow agent worktrees (transient)
.claude/worktrees/

# Vendored WebView2 fixed-version runtime (downloaded for locked-down Windows builds)
src-tauri/Microsoft.WebView2.FixedVersionRuntime.*/
