{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "diagram-viewer",
  "description": "Minimal capability for read-only diagram popout windows. Only core:default is needed for the initialization script injection — no dialog, fs, or shell permissions.",
  "windows": ["diagram-*"],
  "permissions": [
    "core:default"
  ]
}
