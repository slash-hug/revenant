{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability granted to the main window. No blanket fs ACL — all file I/O is routed through Rust IPC commands (A7/C16). No shell permissions — the app never spawns OS processes from the webview. The dialog permission backs the welcome screen's native 'Open file…' picker only.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-save"
  ]
}
